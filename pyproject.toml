[project]
name = "promo-publisher"
version = "0.1.0"
description = "Telegram client for scheduled publishing in approved promotional chats"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv",
    "telethon",
]